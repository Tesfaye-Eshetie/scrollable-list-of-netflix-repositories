import styled from 'styled-components'

export const Card = styled.div`
  width: 800px;
  max-width: 100%;
  padding: 0 20px;
  margin: 0 auto;
  max-height: 100%; 
  overflow: auto;
  &:hover {
    opacity: 0.9;
    transform: scale(0.98);
  }
`